@model IEnumerable<Job_Entity.Models.Utilisateur>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Personne.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MotPasse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DernierVisit)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Personne.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MotPasse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DernierVisit)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDUtilisateur }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDUtilisateur }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDUtilisateur })
        </td>
    </tr>
}

</table>
